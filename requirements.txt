# India Strategy Screener — Python dependencies
# Install:  pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
fastparquet>=2024.2     # parquet engine for data/prices.parquet

requests>=2.28          # NSE bhavcopy download (update_data.py)
yfinance>=0.2.40        # fundamentals snapshot + multi-year financials (Coffee-Can / CANSLIM)

# Optional — only needed for intraday-fresh live prices via Fyers:
# fyers-apiv3>=3.1
# pyotp>=2.9            # headless TOTP login
